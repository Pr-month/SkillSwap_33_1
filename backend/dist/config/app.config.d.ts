export declare const appConfig: (() => {
    port: number;
    environment: string;
    hashSalt: string | number;
}) & import("@nestjs/config").ConfigFactoryKeyHost<{
    port: number;
    environment: string;
    hashSalt: string | number;
}>;
